<head>
  <style type="text/css">
    body {
      background: #1a6875;
      font-family: Arial, Helvetica, sans-serif;
    }
    .left-side {
      max-width: 65%;
      text-align: right;
      position: absolute;
      top: 15%;
      left: 0;
    }
    .title {
      color: #fff;
      font-size: 5em;
    }
    .sub-title {
      color: #fff;
      font-size: 1.3em;
    }
    .right-side {
      width: 15em;
      height: 15em;
      background: #d0e64c;
      border-radius: 50%;
      position: absolute;
      top: 10%;
      right: 5%;
    }

    ul {
      list-style: none;
      margin: 0px;
      padding: 0px;
      color: #fff;
      font-size: 1.3em;
  }
  </style>
</head>
<body>
  <div class="left-side">
    <h1 class="title">공지</h1>
    <p class="sub-title">아주대 😊</p>
    <ul class="notice_list">
      {{#each notices}}
      <li>{{this.title}} ({{this.date}})</li>
      {{/each}}
    </ul>

  </div>
  <div class="right-side">
    <div class="round-image"></div>
  </div>
</body>